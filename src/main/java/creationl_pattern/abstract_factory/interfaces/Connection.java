package creationl_pattern.abstract_factory.interfaces;

/**
 * database on function is connection
 * when connection established it need to say i connected
 */
public interface Connection {
    void connection();
}
